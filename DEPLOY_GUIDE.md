# Deployment guide

Use `GITHUB_PAGES_RELEASE.md` for the complete free publishing steps and release checklist.

Use `FAMILY_TEXT_MESSAGE.md` for the exact message and iPhone/Android home-screen instructions to send after the public link exists.

These directions are kept outside the learning app intentionally.

